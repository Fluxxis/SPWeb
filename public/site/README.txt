Макет страницы входа (статический).

Куда положить обезьяну:
  public/obezyana/obezyana.png

Как открыть правильно (НЕ двойным кликом по index.html, иначе у SPA часто ломается, но тут можно и так):
  1) Самый надежный способ: локальный сервер
     python -m http.server 8000
     затем открыть http://localhost:8000

Это только интерфейс. Реальной авторизации нет.
